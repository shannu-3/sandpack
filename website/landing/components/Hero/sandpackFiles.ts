export const app = `import { createStitches } from '@stitches/react';
import { motion } from 'framer-motion'

const { styled } = createStitches({})

export default function App() {
  return (
    <div>
      <h1>wip.</h1>
    </div>
  )
}
`;
